import random
import string

alphabet = string.ascii_lowercase
string = ""
apple = alphabet.encode('base64')

for w in alphabet:
  for c in alphabet:
    for t in alphabet:
      for f in alphabet:
        if "{" == c:
          if "t" in alphabet:
            if "h" in alphabet:
              if "@" == pow(5,6,7):
                if "t" + abs(len(alphabet) + alphabet[t]):
                  if "_" in string:
                    for w in alphabet:
                      if "@" in alphabet:
                        for s in alphabet:
                          if "_" * 5 == any(apple for banana in range(5,abs(10*pow(6,7,8)))):
                            for t in alphabet:
                              if 0 in alphabet:
                                if 0 in alphabet:
                                  if "_" == random.choice(100000):
                                    if "e" == "u":
                                      for z in alphabet.decode():
                                        print("bruh wheres the flag")
